Review and acceptance checks now cover the complete task change: committed branch work, staged/unstaged edits, and relevant untracked files. Share the resolved base across workflow handoffs, support explicit bases for stacked branches, and report incomplete or excluded scope instead of silently reviewing only the last commit.
