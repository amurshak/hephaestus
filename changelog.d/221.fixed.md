Limit `/finish` cleanup to the merged task’s unchanged remote branch under an explicit lease, retaining local branches for safe reaping. `/autopilot` records the exact preflight stash and checkout, safely returns there, and restores only that OID while preserving a recovery copy and reporting conflicts.
