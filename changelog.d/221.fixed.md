Limit `/finish` cleanup to the merged task’s unchanged, unoccupied branches with atomic tip checks. `/autopilot` records the exact preflight stash and checkout; restoration preserves unrelated stashes, retains a recovery copy, and reports conflicts instead of swallowing them.
