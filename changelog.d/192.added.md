Drift guards for `loop.sh`'s unattended flags (#192): each `verify-*-load.sh` now asserts the approval/headless flags its harness's dispatch entry depends on (`codex exec --help` checked separately from the top level), and `loop.sh` preflights its own dispatch table at startup — deriving the flags from the entry's argv and exiting loudly before the lock instead of hanging on iteration 1 when a harness renames one. `HEPH_NO_PREFLIGHT=1` skips the runtime check.
