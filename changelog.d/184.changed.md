Agent definitions moved to `.ai/agents/` — the canonical, harness-neutral home alongside the workflows, conventions, and `models.conf` (#184). `.claude/agents/` is now a generated render target like every other harness directory: still committed and still Claude Code's discovery path, but produced by `scripts/sync-agent-adapters.sh` with the same `--check` drift gate and stale sweep, so a hand-edit there can no longer silently become the source. The four other generators read `.ai/agents/`.
