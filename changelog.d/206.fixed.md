**collect-changelog preview** (#206): `--preview` now merges the hand-written `## Unreleased` body exactly as release mode does, and both modes report fragment and legacy-entry counts — previously the preview omitted legacy entries entirely, under-reporting the 2.2.0 cut by five entries in the one safeguard before the fragment-deleting fold.
