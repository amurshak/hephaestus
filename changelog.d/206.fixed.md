**collect-changelog preview** (#206): `--preview` now merges the hand-written `## Unreleased` body exactly as release mode does — previously it omitted legacy entries entirely, under-reporting the 2.2.0 cut by five entries in the one safeguard before the fragment-deleting fold. Both modes report fragment and rendered-legacy-entry counts, preview stdout is byte-identical to the body release splices, and preview warns where the release would refuse (missing `## Unreleased`, already-released version).
