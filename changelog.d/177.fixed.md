Test suite now passes under a byte-only locale. `check_composition.sh` matched the README tree's branch glyphs with a bracket expression, which degrades to raw bytes under `LC_ALL=C` and collapsed every sub-child into a top-level one; the Hermes description check sized strings with `${#var}`, so the 3-byte ellipsis read as three characters over the cap. Both now measure in bytes explicitly, `sync-codex-adapters.sh` truncates in bytes so a generated adapter can no longer depend on `$LANG`, and CI runs the suite a second time under `LC_ALL=C`.
