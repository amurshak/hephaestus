`install.sh --changelog-fragments` over a foreign `changelog.d/` now names the migration path — fold pending fragments with the tool that wrote them (e.g. `scriv collect`) — and the README documents the recipe, verified against scriv's real conventions (#167)
