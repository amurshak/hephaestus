`verify-cursor-load.sh`'s header now quotes the spawn form Step 6 actually runs — `cursor-agent -p "<prompt>"` — instead of the pre-#169 bare positional, keeping check 1 scoped to the positional half with `-p` verified separately (#190)
