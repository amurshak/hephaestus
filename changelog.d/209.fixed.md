**collect-changelog.sh** (#209): a fragment left conflicted mid-merge no longer publishes its conflict markers — validation greps every fragment for `<<<<<<< `/`>>>>>>> ` lines, so `--check` and a release both fail before anything is written instead of folding the markers verbatim and deleting the conflicted file.
