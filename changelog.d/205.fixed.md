**collect-changelog.sh** (#205): a locally-modified tracked fragment no longer survives the release fold — removal forces `git rm -f` (the on-disk content is exactly what was just published), the closing summary refuses to claim a clean fold while any fragment remains in `changelog.d/`, and a release dies up front if CHANGELOG.md already has the version's section, since re-splicing it would duplicate entries.
